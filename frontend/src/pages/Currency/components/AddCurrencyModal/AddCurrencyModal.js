import Modal from '../../../../components/UI/Modal/Modal.js'

const AddCurrencyModal = ({ closeCurrencyModal }) => {
    return(
        <Modal closeModal={closeCurrencyModal}>
            Peeacces son
        </Modal>
    )
}

export default AddCurrencyModal